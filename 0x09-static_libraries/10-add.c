#include <stdio.h>
#include "main.h"

/**
 * add - A function that adds two integers and returns the result
 * @a: the first number
 * @b: the second number
 * Return: the result of the sum
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
